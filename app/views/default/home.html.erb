<h1>Voici tous les gossips :</h1>

<ul>
<% @gossips.each do |gossip|%>
<%
=begin%>
 <li>
  <span><b> Titre :</b></span>
  <%= gossip.title%>
  <br />
  <span><em> Auteur :</em></span>
  <%= gossip.user.last_name%>
  <br />
  <a href="<%=gossip_path(gossip.id)%>"> Afficher</a>
  
  <br />
  <br />
</li> 
<%
=end%>

<div class="card w-50 ">
  <div class="card-body">
    <h5 class="card-title"><b> Titre : </b><%= gossip.title%></h5>
    <p class="card-text"><em> Auteur : </em><%= gossip.user.last_name%></p>
    <a href="<%=gossip_path(gossip.id)%>" class="btn btn-primary btn-dark">Afficher</a>
  </div>
</div>
<% end %>


</ul>>