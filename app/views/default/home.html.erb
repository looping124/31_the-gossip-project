  <div class="container my-temoignages">
    <div class="row my-row ">
<h1 class="row justify-content-around">Voici tous les gossips :</h1>
<a class="btn btn-secondary m-4" href="<%=new_gossip_path%>" role="button">Créer un gossip</a>
</div></div>
<div class="container my-points">
  <div class="row  justify-content-between my-rowrow">

<% @gossips.each do |gossip|%>
<%
=begin%>
 <li>
  <span><b> Titre :</b></span>
  <%= gossip.title%>
  <br />
  <span><em> Auteur :</em></span>
  <%= gossip.user.last_name%>
  <br />
  <a href="<%=gossip_path(gossip.id)%>"> Afficher</a>
  
  <br />
  <br />
</li> 
<%
=end%>

<div class="card col-md-6 mt-2" style="width: 40rem;">
  <div class="card-body">
    <h5 class="card-title"><b> Titre : </b><%= gossip.title%></h5>
    <p class="card-text"><em> Auteur : </em><a href="<%=user_path(gossip.user.id)%>" class=""><%= gossip.user.last_name%></a></p>
    <p class="card-text"><em> Ville : </em><a href="<%=city_path(gossip.user.city.id)%>" class=""><%= gossip.user.city.name%></a></p>
    <a href="<%=gossip_path(gossip.id)%>" class="btn btn-dark">Afficher</a>
  </div>
</div>
<% end %>


</div></div>